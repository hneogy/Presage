# Cloudflare Pages config — used if deploying via `wrangler pages deploy site`
# rather than the dashboard. Either path works; this file just makes the CLI
# path one-shot.
name = "presage"
pages_build_output_dir = "site"
compatibility_date = "2025-01-01"
